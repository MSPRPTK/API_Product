services:
  mongo:
    image: mongo:latest
    container_name: mongodb
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db
    networks:
      - backend

  nodejs:
    build:
      dockerfile: Dockerfile

    networks:
      - backend
    depends_on:
      - mongo

volumes:
  mongo_data:

networks:
  backend:
